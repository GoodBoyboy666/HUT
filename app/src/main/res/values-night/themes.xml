<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HUT" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimaryDark">@color/black</item><!--顶部导航栏 -->
        <item name="colorPrimary">@color/white</item><!--主题颜色 （包含部分字体）-->
        <item name="colorAccent">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:positiveButtonText">@color/white</item>
        <item name="android:fontFamily">@font/minecraft</item>
    </style>

</resources>